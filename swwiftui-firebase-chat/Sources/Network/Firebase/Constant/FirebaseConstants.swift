struct FirebaseConstants {
    
    struct ErrorMessage {
        static let couldNotFindFirebaseUID = "Could not find firebase uid"
    }
    
    struct Collection {
        static let users = "users"
    }
    
}
